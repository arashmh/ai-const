import React from 'react';

/**
 * This component has been removed.
 * It was a non-functional placeholder and is not compatible with the current task-based protocol structure.
 * This stub is left to prevent build errors and can be fully deleted in the future.
 */
const ProtocolFlowGraph: React.FC<{ protocol: any }> = () => {
  return null;
};

export default ProtocolFlowGraph;
